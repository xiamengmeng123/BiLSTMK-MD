environment:pytorch=2.4.0   cuda=12.1  python=3.9  gromacs=2019.6 mpi并行版

1.Hyperparameter Optimization with Bayesian Optimization
srun -N 1 -p $gpu_partition --cpus-per-gpu=4 --gpus-per-node=1  python run_bayesian_fANOVA.py 

2.Batch submit jobs with the Final Optimal Parameters
2.1 for i in `seq 0 20`;do cp -rf sub.sh sub$i.sh;done

2.2 Extract system parameters and pass them to each sub$i.sh
for i in `seq 0 20`;do srun -N 1 -p $partition python ready_param_for_sub.py $i;done
#srun -N 1 -p cp1 python ready_param_for_sub.py  $lambda_param

2.3 Verify all parameters carefully before job submission
rm -rf merged_sub.sh && cat sub{0..27}.sh > merged_sub.sh
sbatch -p $gpu_partition  merged_sub.sh 

2.4 Evaluate predicted free-energy values
gmx_mpi bar -f *.xvg -o -oi -oh

